rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Tạm thời cho phép tất cả để test - KHÔNG AN TOÀN CHO PRODUCTION
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
